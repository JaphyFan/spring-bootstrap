server:
  port: 7003

spring:
  application:
    name: cloud-bill

management:
  endpoints:
    web:
      exposure:
        include: "*"
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/

order.service.url: http://localhost:7002